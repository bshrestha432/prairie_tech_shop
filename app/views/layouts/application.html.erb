<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Prairie Tech Shop" %></title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>

    <!-- Include Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom Styles -->
    <style>
      body {
        background-color: #867f7f;
        color: #0e0b0b;
      }

      h1, h2, h3, h4, h5, h6 {
        color: #141411;
        border-color:#141411;
      }

      .navbar {
        background-color: #665c5c;
      }

      .btn-primary {
        background-color: #5a2b0b;
        border-color: #6e3005;
      }

      .btn-primary:hover {
        background-color: #e6b800;
        border-color: #e6b800;
      }

      .pagination {
        justify-content: center;
        margin-top: 20px;
      }

      .pagination .page-item.active .page-link {
        background-color: #ffcc00;
        border-color: #ffcc00;
      }
      .pagination .page-link {
        color: #fff;
        background-color: #efe5f7;
        border-color: #444;
      }
    </style>
  </head>

  <body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/" style="font-size: 36px; color: #ffcc00;">Prairie Tech Shop</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <% if user_signed_in? %>
            <!-- Edit Province link, visible only if user is logged in -->
            <li class="nav-item">
              <%= link_to 'Edit Province', edit_user_registration_path, class: 'btn btn-secondary mx-2' %>
            </li>
            <!-- Cart link, visible only if user is logged in -->
            <li class="nav-item">
              <%= link_to 'Cart', cart_path, class: 'btn btn-primary mx-2' %>
            </li>
            <!-- Logout button -->
            <li class="nav-item">
              <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-primary' %>
            </li>
          <% else %>
            <!-- Login and Sign Up buttons -->
            <li class="nav-item">
              <%= link_to 'Login', new_user_session_path, class: 'btn btn-primary mx-2' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Sign Up', new_user_registration_path, class: 'btn btn-primary mx-2' %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

    <div class="container mt-5">
      <%= yield %>  <!-- The main content will be injected here -->
    </div>

    <!-- Include Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
