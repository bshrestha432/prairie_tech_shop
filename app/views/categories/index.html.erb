<h1>Categories</h1>

<% @categories.each do |category| %>
  <div class="category">
    <%= link_to category.name, category_path(category) %>  <!-- Link to show category products -->
  </div>
<% end %>
